11/1
Strong, Elite 몬스터 프리팹에 겹침 방지용 콜라이더 추가
타일맵에 나무 장식물 추가

- Normal 몬스터와 동일하게 겹침 방지용으로 Strong, Elite 몬스터도 IsTrigger를 끈 콜라이더 추가. (플레이어 감지용 콜라이더보다 훨씬 작게 몸통 중앙에 작게 추가)
- 만약 플레이어 감지용 콜라이더와 같은 크기로 설정하면 서로 플레이어를 쫒아간다고 뭉쳐지면 IsTrigger로 인해 이동이 불가 혹은 느려짐.


- 타일맵에 장식용 나무 추가.
- 나무 프리팹의 스프라이트 에디터를 수정.
   1. 나무 기둥과 위의 나뭇잎을 분리
   2. Frist 라는 Sorthing Layer를 추가해 나무 프리팹에 적용
   3. 나무기둥에만 IsTrigger를 끈 콜라이더 추가. (나무 기둥은 통과하지 못하게)
   4. 위의 나뭇잎은 그대로 유지, First Sorthing Layer 덕분에 나뭇잎이 플레이어나 몬스터를 가림

**내일은 만약 나무 뒤에 가려질 수 있는 아이템에 대해 대책을 생각해보자**



11/2
나무 뒤에 스폰될 아이템을 대비해 나무 뒤에서 가려지면 추가로 보여질 아이템 이미지 추가, 하지만 실패
결국 단순하지만 확실한 나무 오브젝트보다 우선의 Sorting Layer로 최우선으로 보여지게 설정

- 타일맵에 있는 나무 때문에 아이템이 스폰되면 나무로 인해 가려질 경우를 대비해야 했어.
- 나무 뒤에 아이템이 스폰될때 다른 아이템 이미지를 보여지게 해서 아이템의 Sorting Layer를 건드리지 않게 작업을 하려고 했어.
- Item 스크립트에 OntriggerStay2D, Exit...가려질때의 아이템의 이미지를 변수에 드래그 연결 등 추가해봤지만 원하는 방향대로 흘러가지 않았어.
- 결국 각종 문제, Item 스크립트 로직 추가 등, 계속된 실패로 단순하게 문제를 해결했어.

- 나무의 Sorting Layer는 Player와 같은 Player 레이어(플레이어는 1, 나무는 2), 아이템은 First라는 Sorting Layer를 추가.
- Sorting Layer는 목록에 아래로 내려갈수록 우선적으로 화면에 먼저 보여져. Layer1 Layer2..3.....4....같이 뒤에 숫자가 높은 목록 일수록 우선으로 화면에 보여지는거지.
- 결국 원하지 않았던 방식을 해버렸어. 이건 나중에 꼭 내가 원하는 방식으로 만들어 보자. **나무 등에 가려지면 아이템의 윤곽선을 보이게 해서 여기에 있다는걸 보이게 해보자**



11/3
Enemy 스크립트의 [Serializable] 에 대한 설명 주석 추가

-  Serializable 이란? (정의)
   1. 정의: C# 클래스(Class)나 구조체(Struct) 위에 붙이는 특별한 속성(Attribute). System 네임스페이스에 속해있어
   2. 핵심 역할: 유니티 엔진에게 "이 클래스를 직렬화(Serialization) 가능한 데이터 구조로 취급해 주세욥" 라고 공식적으로 요청하는 허가증 역할이야

- Enemy 스크립트에 Serializable이 필요한 이유 (문제 해결)
   1. 일반적으로 유니티는 MonoBehaviour를 상속받은 스크립트의 public 변수만 인스펙터에 표시하고 저장할 수 있어,
      하지만 EnemyStats처럼 MonoBehaviour를 상속받지 않은 일반 C# 클래스를 사용하려면 이 허가증이 필수야
   2. Serializable이 없을 때 발생하는 문제:
      2-1. 인스펙터 미노출: public EnemyStats NormalStats; 같은 변수가 유니티 인스펙터에 아예 표시되지 않아
      2-3. 데이터 미저장: 인스펙터에서 값을 수정하려고 해도 유니티가 그 데이터를 저장하지 못해, 프로젝트를 껐다 켜거나 다시 실행하면 코드가 정의한 기본값으로 초기화돼

- Serializable 사용의 이점 (EnemyStats 적용 사례)
   1. 시각적 그룹화 및 편리한 편집: NormalStats, StrongStats 변수 아래에 MoveSpeed, AttackDamage 등의 필드들이 논리적인 그룹으로 묶여서 인스펙터 가독성이 극대화되고, 기획자가 밸런싱 작업을 하기 쉬워져
   2. 데이터 영구 저장: 인스펙터에서 수정한 모든 값들이 프리팹 파일에 안전하게 기록(직렬화) 되어 영구적으로 유지돼
   3. 높은 재사용성: EnemyStats라는 스탯 템플릿(설계도) 을 단 한 번만 정의하고, 이 템플릿을 NormalStats, StrongStats라는 여러 인스턴스로 만들어서 재사용함으로써 코드의 중복을 제거하고 확장성을 확보할 수 있어

**결론 (Serializable의 가치): Serializable은 데이터와 코드를 분리하는 유니티의 핵심 원칙이자, 협업 환경에서 기획자와 개발자 모두의 생산성을 극대화하는 가장 기초적인 설계 도구야**



11/4
Enemy 스크립트의 스탯 시스템의 '데이터 직렬화' 메커니즘 완벽 이해 (배경: Enemy스크립트의 [Serializable] 클래스와 switch 분기 로직에 대한 근본적인 이해 부족 해결)

- [Serializable]의 역할 재정립
   1. EnemyStats와 같은 MonoBehaviour를 상속받지 않은 일반 C# 클래스를 유니티 인스펙터에 노출시키고 **프리팹 파일에 영구적으로 저장(직렬화)**하기 위한 필수 태그임을 확인. (데이터 영속성 확보)
   2. 비유하면 MonoBehaviour의 하청업체 역할을 수행하며, Enemy 클래스에 포함되어야만 저장 권한을 얻어

- 데이터 설정 흐름 파악
   1. Editor 단계: [Serializable] 덕분에 NormalStats, StrongStats, EliteStats 에 수정한 값이 프리팹에 저장돼
   2. Runtime 단계 (핵심!): Start()에서 호출되는 SetEnmeyStats() 함수가 실행됨
   3. 세부 프로세스 (SetEnmeyStats 내부 로직):
      3-1. 타입 선택: switch(enemyType)를 통해 인스펙터에서 설정된 타입에 맞는 스탯 덩어리(NormalStats 등)를 selectedStats에 할당 (데이터 선택) 
      3-2. 난이도 및 최종 적용: EnemyDifficulty.Instance를 통해 난이도 보정을 거친 후, 그 결과값을 currentMoveSpeed, currentAttackDamage 등 실제 게임 로직에서 사용될 내부 변수에 최종적으로 담는다 (데이터 가공 및 할당)

- switch 문의 정확한 역할 이해:
   1. 기능: 인스펙터에서 설정한 EnemyType (Normal, Strong 등) 명찰을 확인하여, 프리팹에 저장된 세 가지 스탯 덩어리(NormalStats, StrongStats, EliteStats) 중 하나를 통째로 선택해서 selectedStats에 할당
   2. 결론: switch 문은 기획 데이터 중 어떤 것을 쓸지 결정하는 최종 분기점이며, 이후 로직은 selectedStats만 참조하여 코드를 간결하게 유지


- 예시) 인스펙터 값 → 게임 적용 6단계 흐름 정리 (인스펙터에서 수정한 값이 게임에 적용되는 과정(데이터 파이프라인)을 명확히 정리)
   1. 데이터 저장: 인스펙터에서 Normal몬스터의 스탯 값 수정 → [Serializable]을 통해 프리팹 파일에 영구 저장
   2. 메모리 로드: 게임 시작 시, 유니티가 프리팹 파일을 읽어 NormalStats 변수(메모리)에 수정된 값을 로드
   3. 함수 호출: 몬스터 오브젝트의 Start() 함수 실행 → SetEnmeyStats() 호출
   4. 타입 선택: SetEnmeyStats() 안의 switch (enemyType) 문이 실행되어 NormalStats를 selectedStats에 할당
   5. 난이도 보정: selectedStats의 값을 EnemyDifficulty를 거쳐 난이도 보정 처리
   6. 최종 적용: 보정된 최종 값이 currentMoveSpeed 등 실제 게임 로직 변수에 할당되어 몬스터 움직임에 반영



11/5
Enemy 스크립트의 불필요한 로직 최종 수정
코드가 한 줄만 있는 함수 간략화

- DealDamageToPlayer() 함수에 있던 playerShield = playerGameObject.GetComponent<PlayerShield>(); , playerHealth = playerGameObject.GetComponent<PlayerHealth>();를 Start() 함수로 옮겼어.
- 내부 컴포넌트는 Awake()에, 외부 스크립트는 Start() 함수에. GetComponent는 비용이 비싼 작업이므로, 매 프레임이나 공격 때마다 호출하는 것을 방지하고, 게임 시작 시 한 번만 호출하여 변수에 저장(캐싱)함으로써 런타임 성능을 극대화함.
   1. Awake()의 내부 컴포넌트 = 가장 먼저 호출. 스크립트가 붙은 같은 오브젝트의 컴포넌트는 항상 준비가 보장되므로, 가장 빠르게 캐싱해서 사용할 준비를 마침
   2. Start()의 외부 스크립트 = Awake가 모두 끝난 후 호출. 씬의 다른 오브젝트들이 자기 자신의 Awake를 통해 준비를 마칠 때까지 기다려서 널 에러를 방지하는 안전 장치 역할
   3. 요약: Awake: 나 자신이 준비하는 시간, Start: 모두가 준비를 마칠 때까지 기다렸다가, 다른 오브젝트와 연결하는 시간


- ApplyTouchDamage()와 OnAttackFinished() 함수는 내부에 코드가 한줄만 있었어, 
  이처럼 **반환 값이 없더라도** (void 함수), **단 하나의 표현식(Expression) 또는 문장(Statement)을 실행**할 때 간결하게 **=>를 쓰는 '표현식 본문 멤버(Expression-bodied Member)'**로 수정했어.
   1. void ApplyTouchDamage() => DealDamageToPlayer();//통합 함수 호출
   2. public void OnAttackFinished() => animator.SetBool("Attack", false);//몬스터의 Animation에서 이 함수를 호출해 공격 종료

- void 함수에 람다식을 적용할 수 있는지에 대해 헷갈릴 수 있는데, C#에서는 두 가지 경우가 가능해.
   1. 값을 반환하는 경우 (Getter): 무조건 return 역할. (예: public int GetX() => _x;)
   2. 값을 반환하지 않는 void 함수: **단 하나의 문장(Statement)**만 실행하는 경우 허용돼


- 로직 안정성 강화: DealDamageToPlayer()에 최후의 IsDead 체크 추가.
- 데미지를 적용하는 결정적인 순간에 playerScript.IsDead를 체크해서 플레이어가 죽은 후에도 애니메이션 이벤트로 인해 몬스터의 공격 함수가 실행되는 잠재적 버그를 방지. (방어적 프로그래밍)



11/6
Player 스크립트 수정.

- 코드 실행 순서(Awake → Start)를 활용하여 안정성을 확보.
   1. GetComponent<>()로 찾는 내부 컴포넌트 (rb, animator, playerHealth 등) 및 [SerializeField]로 연결된 외부 참조 (gameOverManager, walkingaudioSource, ScoreTextUI)의 모든 Null 체크를 Awake() 함수로 이동
   2. Start() 최소화: Start()는 이제 UI 초기값 설정 (ScoreTextUI.text = ...) 등 컴포넌트가 모두 준비된 후에 실행할 최초 동작만 담당하도록 분리


- GameOverManager 스크립트 참조 방식 최적화 (캡슐화(private)를 유지하며 인스펙터 연결의 편리성을 사용)
   1. 변수를 [SerializeField] private GameOverManager gameOverManager; 로 선언하여, public의 불안정성 없이 인스펙터 연결을 허용
   2. FindAnyObjectByType()을 제거하여 게임 시작 시 성능 부하를 줄임 (씬에 오브젝트가 많아질수록 검색 시간이 선형적으로 늘어나 **프레임 저하**의 가능성이 있어)
  	

- 플레이어 사망 시 메인 루프 (Update/FixedUpdate) 효율 극대화.
   1. 목표: 플레이어가 사망(IsDead = true)했을 때 불필요한 연산을 즉시 중단
   2. Update() 최상단 체크: Update() 함수의 가장 첫 줄에 if (IsDead) return;을 배치시켜 플레이어가 사망 시 입력 처리, 사운드, 애니메이션 로직 전체를 즉시 건너뛰도록 수정했어
   3. FixedUpdate() 간결화: PlayerDie() 함수에서 rb.simulated = false;를 실행하므로, FixedUpdate()의 중복된 if (IsDead) 블록을 제거하고 순수 이동 로직 한 줄만 남겼어


- PlayerDie() 함수 로직 명확화 및 버그 수정
   1. 플레이어 사망 처리 함수가 자신의 핵심 책임 (상태 변경 및 모든 활동 중단) 에만 집중하도록 정리했어
   2. 불필요 코드 제거: horizontalInput = 0; 등 Update() 함수에서 이미 처리되는 입력 변수 초기화 코드를 삭제
   3. 버그 수정: 사망 직후 걷기 사운드가 멈추지 않는 버그를 확인했어. PlayerDie() 함수 내부에 if(walkingaudioSource != null && walkingaudioSource.isPlaying) walkingaudioSource.Stop(); 로직을 추가해서,
   사망 즉시 이동 시 사운드를 중단 하도록 조치했어



11/7
닿으면 지속 데미지를 받는 가시밭을 타일맵에 추가
Debug.Log, Debug.LogWarning, Debug.LogError 차이 

- 가시 프리팹의 크기와 범위가 작아서 5~6개로 복사해 부모 오브젝트 'SPIKE'를 만들어 그 안에 여러개의 가시 프리팹 자식으로 두고 프로젝트 창에 이동시켜서 하나의 프리팹으로 완성.
   1. 부모 오브젝트에만 BoxCollider2D를 추가하고 통과하기 위해 IsTrigger를 켜고. 이 범위 안에 플레이어가 들어오면 데미지를 입게 하고 범위 밖으로 나가면 도트 데미지를 주게 했어.

- 타일맵에 닿으면 데미지를 받고 벗어나도 n초간 도트데미지를 주는 가시 바닥 추가.
   1. Spike 스크립트를 생성. OntriggerEnter와 Exit, 코루틴을 사용
   2. 코투틴 전용 변수 private Coroutine spikeDamageCoroutine; , private Coroutine debuffCoroutine; 선언
      2-1. 이 두 변수는 코루틴에서만 사용하는 코루틴의 실행과 중지를 제어하는 코루틴 전용 변수야
      2-2. spikeDamageCoroutine = 타일 위에 있을 때의 데미지 처리 코루틴
      2-3. debuffCoroutine = 타일을 나간 후의 디버프 처리 코루틴


- 각 디버그 (Log, LogWarning, LogError)

- Debug.Log = 특정 함수가 호출되었는지, 루프가 몇 번 실행되었는지 등 코드가 의도대로 잘 작동하고 있음을 확인하는 데 사용해 [게임 흐름 추적, 변수 값 출력]
- Debug.LogWarning = 게임은 멈추지 않고 계속 실행되지만, 나중에 버그나 성능 저하를 유발할 수 있는 상황을 알려줘. 콘솔에서 노란색 필터로만 따로 걸러서 볼 수 있어, 정리해야 할 코드를 찾기 쉬워. [비정상적인 데이터, 컴포넌트 누락 시]
- Debug.LogError = 프로그램 실행을 멈추거나 치명적인 문제 (예: NullReferenceException)를 알릴 때 사용. 즉각적인 조치가 필요한 심각한 에러야. [return 바로 전, 심각한 로직 오류]
**내가 Spike 스크립트의 OnTriggerEnter2D 함수에서 if (playerShield == null) { Debug.LogError(...) }를 사용한 것이 아주 적절해! 치명적인 Null 에러를 미리 방지하고 개발자에게 명확하게 알리는 최고의 방법이야.**



11/8
Spike 스크립트의 데미지 등 강화를 ObstacleDifficultyManager 스크립트에 통합
Obstacle 관련된 스크립트 이름 변경

- Spike 스크립트의 데미지 변수들은 외부에서 받는 역할만 하고 데미지, 디버프 데미지, 20초마다 데미지 강화는 ObstacleDifficultyManager 스크립트에서 관리 하도록 했어.
   1. ObstacleFireBall 스크립트처럼 이 안에도 FrieBall의 데미지와 속도, 등이 있지만 이 변수들은 ObstacleDifficultyManager 스크립트에서 정한 값을 우선으로 적용하게 해서 ObstacleFireBall 스크립트의 변수는,
   매니저 스크립트에서 보낸 값을 받는 역할만해


- Obstacle, ObstacleSpawner 스크립트, Bolt의 이름을 바꿨어. Spike 스크립트도 ObstacleDifficultyManager 스크립트의 관리를 받으니 매니저 스크립트는 그대로 두고 나머지는 이름을 바꿨어.
   1. Bolt(발사체 프리팹)=FireBall
   2. Obstacle=ObstacleFireBall.cs
   3. ObstacleSpawner=ObstacleFireBallSpawner.cs
   4. 스크립트 이름을 바꿨으니 다시 서로 연결을 하고 각 함수와 변수 이름도 FireBall에 맞게 변경했어
- ObstacleFireBallSpawner 스크립트에서 InvokeRepeating 함수 인자로 사용되던 함수 이름 불일치 오류를 수정하여, 발사체 스폰 시스템의 안정성을 확보.
- ObstacleDifficultyManager 스크립트의 모든 Getter 함수 (GetCurrentSpikeDamage())를 람다식 (=> 구문) 으로 변경하여 코드를 극도로 간결하게 만들고 가독성을 높였어.
   1. 한줄만 있는 함수라 가독성과 공간을 위해 람다식으로 수정
   2. 수정 전에는 return이 있는 함수지만 =>이 return을 역할을 해
   3. 람다식 구문에서 => 기호는 **오른쪽 표현식의 결과를 자동으로 반환(return)한다** 는 의미를 내포하고 있어. 따라서 중괄호({})와 return 키워드를 명시적으로 쓰지 않아도 반환 기능은 그대로 작동해

**내일은 ObstacleFireBall 스크립트의 GetComponent 등 정리와 또, ObstacleFireBall과 Enemy 스크립트 추가 수정 필요. 데미지를 받을때 playerShield 스크립트 내부에 playerHealth로 데미지를 이전 시키는 로직이 있으니까**



11/9
ObstacleFireBall, Enemy, PlayerShield 스크립트의 GetComponent<playerHealth> 삭제 (불필요한 로직), 설명 주석 추가

- 코드 최적화 원칙: 캐싱
   1. 참조 안정성: 프리팹(Prefab)처럼 동적으로 생성되는 오브젝트는 Awake()/Start() 시점에 코드를 통해 필요한 컴포넌트(예: 플레이어 스크립트)를 찾아 **한 번만 private 변수에 저장(캐싱)**하여 사용한다
   2. 성능 최적화: Update()나 FixedUpdate() 같은 프레임마다 반복 실행되는 함수 내부에서는 GetComponent()나 GameObject.Find() 같은 고부하 함수를 절대 사용하지 않고, 캐싱된 변수를 재사용하여 CPU 부하를 최소화한다
   3. 직접 연결 우선: 씬에 항상 존재하는 오브젝트는 [SerializeField] 변수를 통해 인스펙터에서 직접 연결(드래그 앤 드롭)하는 것을 최우선으로 한다

- 다른 스크립트에서도 불필요한 공간이 있으면 정리하고 각 스크립트에 GetComponent, 선언변수를 쓰는 이유를 주석으로 설명했어.
   1. ObstacleFireBall는 충돌 후 즉시 파괴되는 '단명 오브젝트'이므로 GetComponent로 찾은 주소를 private 변수에 따로 저장(캐싱)할 필요가 없기 때문.
   2. Enemy처럼 반복 사용되는 오브젝트만 캐싱이 필요해



11/10
타일맵 내에 랜덤 위치에 등장하는 SpikeSpawn 스크립트 추가
장애물 난이도 동적 스케일링 시스템 (스파이크/발사체 동기화) 구현

- 오늘 작업은 게임의 핵심인 `난이도 조절 시스템` 을 유기적으로 만들고, 모든 장애물이 중앙 관리자(ObstacleDifficultyManager 스크립트)의 지시를 따르도록 연결하는 것이었어. 
  특히, 레벨업 시 발생할 수 있는 동기화 오류를 잡아내는 디버그 로직까지 추가해서 시스템의 안정성을 극대화했어.

- SpikeSpawn 스크립트 추가.
- 타일맵 상에 가시 함정을 랜덤하게 생성하고 자동으로 사라지게 하는 시스템 구축.
   1. 타일맵 좌표를 인식하여 플레이어가 예상하기 어려운 위치에 무작위로 가시가 생성되도록 로직을 구현했어
   2. 생성된 가시가 일정 시간 후에 스스로 파괴되는 자동 소멸 기능을 추가하여 자원 관리를 효율화했지.

- Spike 스크립트(난이도 동기화)
- 가시의 데미지 및 디버프 지속 시간이 난이도 관리자(ObstacleDifficultyManager)에게 종속되도록 변경.
   1. 스크립트 내에 하드코딩된 데미지 변수를 완전히 제거했어
   2. 대신, ObstacleDifficultyManager.Instance.GetCurrentSpikeDamage()를 호출해서 실시간으로 현재 난이도 레벨에 맞는 값을 받아오도록 연결했어. 이로써 난이도 변경 시 UI 재시작 없이도 즉시 가시에 적용돼

- ObstacleDifficultyManager 스크립트(핵심 시스템 완성)
- 목표: 4가지 개별 강화 항목(발사체 속도, 생성 주기, 데미지, 가시 난이도)을 단일 currentLevel 변수에 동기화하고, 디버깅을 위한 상세 로그를 추가.
   1. 가시 난이도 관리(20초마다 데미지 증가) 변수 추가: 가시의 데미지 및 디버프 시간 증가를 위한 타이머와 변수(예: timeSinceLastSpikeIncrease, currentSpikeDamage)를 멤버 변수로 완벽히 통합했어
   2. 중앙 레벨업 로직 구현: if (levelUpJustHappened) 블록 내에서 currentLevel++와 UpdateLevelText() 함수를 호출해 레벨업이 중복되지 않고 깔끔하게 1씩 증가하도록 보장했어 	
   3. 로컬 플래그(지역변수 플래그. fireballSpeedIncreased, intervalDecreased 등)를 사용하여 이번 프레임에 어떤 개별 강화가 성공했는지 Update()함수 내 각 장애물 난이도 증가 if문에 정확하게 추적했어
   4. 최종 로그 메시지에서 '완벽 동기화 성공' 메시지와 '부분 동기화 됨' (지연/미발동 항목 명시) 메시지를 구분하여 출력하도록 구현했어. (이 로직은 나중에 성능 분석이나 버그 추적에 매우 중요한 역할을 해)
      4-1. if (allSucceeded) Debug.Log($"★★★ 전체 장애물 레벨업! (Lv.{currentLevel}) 완벽 동기화 성공! ({successful})");
      4-2. else Debug.LogWarning($"★★★ 전체 장애물 레벨업! (Lv.{currentLevel}) 부분 동기화 됨. ★★★\n[성공 항목]: {successful.Trim()}\n[지연 항목]: {missed.Trim()}");


**내일은 ObstacleDifficultyManager스크립트에서 발사체(FireBall)의 강화 시간을 통합하자, 속도, 데미지, 발사주기에 각각 시간 간격이 있어. Spike처럼 하나로 통합하자**
**또 10일까지 했던 모든 기능 (가시, ObstacleDifficultyManager에 가시 데미지 관리, SpikeSpawn에서 타일맵 내에 가시 생성,파괴)을 포트폴리오에 적고 다시 빌드해서 git에 올리고 포폴에 다운로드 링크도 다시 추가해주자!**



11/11
122회 헌혈
가시 관련 스크립트 재정리

- ObstacleDifficultyManager 스크립트가 가시의 생성 시간을 관리해.  (동적 난이도 조절을 위해 ObstacleDifficultyManager 스크립트가 중앙 관리형태)
- SpikeSpawn 스크립트는 이젠 가시가 사라지는 시간만을 담당. (ObstacleDifficultyManager 스크립트의 currentSpikeSpawnInterval 변수를 가져와 다음 가시 스폰 주기를 확인)
- Spike 스크립트는 ApplyDebuffDamage() 함수에서 디버프의 시간을 timer = postExitDuration; 으로 timer 변수 하나로 난이도 값을 가져와. ( while (timer > 0) {...} )

**내일은 포트폴리오에 가시생성과 20초마다 데미지, 스폰 주기 감소 등을 설명하는 글을 적자! 오늘 헌혈도 수고 많았어!**



11/12
장애물 난이도(ObstacleDifficultyManager스크립트)의 중앙 관리 시스템 통합 및 스크립트 책임 분리
git, 포트폴리오의 유뷰트 시연 영상 업데이트

- 이제 ObstacleDifficultyManager 스크립트는 가시와 FireBall의 데미지, 생성 시간, 발사속도 등을 전부 관리하게 해서 말 그대로 `관리자`의 역할을 하게 될거야.

- ObstacleFireBall 스크립트는 이제 발사체의 생성 위치에 따라 좌우 모습 변경, 플레이어에게 닿았을때 파괴(Destroy(gameObject);)와 플레이어에게 줄 데미지를 관리자의 GetCurrentFireBallDamage 함수를 받아서 적용.
- ObstacleFireBallSpawner 스크립트는 맵 좌우 끝에서 랜덤 위치에 생성 후 발사만을 담당, 관리자의 GetCurrentSpawnInterval 함수를 받아 생성 주기를 확인.

- Spike 스크립트는 OnTriggerEnter, Exit로 플레이어가 가시를 밟으면 GetCurrentSpikeDamage함수를 받아 데미지 적용, GetSpikeDamageInterval 함수도 받아 데미지를 받는 시간도 적용
   1. 가시를 벗어나면 Exit에서 GetSpikeDamageInterval()를 받아 디버프 데미지를 받는 시간, GetCurrentDebuffDamage()를받아 디퍼브 데미지를 받아

- SpikeSpawn 스크립는 이제 가시가 타일맵 내 랜덤한 위치에서 스폰되게 하고 관리자 스크립트에서 정한 가시의 등장, 사라지는 시간을 받아.
   1. GetCurrentSpikeSpawnInterval() 받아 생성 시간을 받아
   2. GetCurrentSpikeDuration()를 받아 가시가 사라지는 시간을 받아	


- 나무, 가시 등이 추가 됐으니 빌드 후 폴더를 압축 후 git에 올리고 다운로드 링크를 포트폴리오에 업데이트했어.
- 시연 영상도 새로운 오브젝트를 보여주기 위해 다시 영상을 찍고 유뷰트에 올린 다음(일부공개) 링크 주소를 Git의 README와 포트폴리오에 다시 올렸어.



11/13
ObstacleDifficultyManager 스크립트 [Header]의 변수 이름 변경

- [Header]에 포함된 각종 변수들의 이름을 변경했어.
- FireBall과 Spike의 모든 [Header] 난이도 변수를 직관적인 이름으로 변경했어. (Base Name, ...Up, ...Down, ...Time)에 따라 리팩토링 완료. (예: FireBallSpawnTime, SpikeDamageTickTime, SpikeLifeTimeUp 등)
- Initial, DecreaseRate, Duration보다 직관적이고 바로 알 수 있게 만들었지. (Header의 이름 덕에 이 변수의 역할이 뭔지 알 수 있지만 더더욱 바로 알 수 있고 직관적일 필요가 있었어)
**가시 난이도 증가 변수명 오타 (SpikeILevelUpTime → SpikeLevelUpTime) 수정 완료**

- 선언 변수의 이름을 바꾸면 다른 함수 등에 있는 변수의 이름도 선언 변수의 이름과 같아야 하잖아. 
   1. 변수 오른쪽 클릭 - 이름바꾸기(Ctrl+R) - 이름변경 - 이렇게 하면 다른 같은 변수들도 이름이 수정돼
   2. 알고 있었지만 하나하나 보고 수정할려는 버릇 때문에 사용하지 못했나봐 ;ㅅ;



11/14
EnemyDifficulty 스크립트 로직 통일 및 변수 정리
EnemySpawn 스크립트 정리

- EnemyDifficulty 스크립트에서 난이도 강화 로직을 프로젝트 전체의 원칙에 맞게 통일하고 불필요한 요소를 제거했어.
- 타이머 방식 통일: 몬스터 스탯 강화 타이머를 총 경과 시간 방식(gameTimer)에서 타이머 리셋 방식(timeSinceLastLevelUp)으로 변경하여 ObstacleDifficultyManager 스크립트와 로직을 통일했어
   1. gameTimer 변수 삭제
   2. timeSinceLastLevelUp 변수 도입 및 20초마다 리셋 로직 적용
- 난이도 강화 주기 통합: Normal 몬스터의 스폰 주기 감소 로직 (10초마다 0.1초 감소)을 완전히 삭제했어.
   1. 관련 변수 3개 삭제: NormalSpawnTimeDown, SpawnTimeDecreaseTime, timeSinceLastDecrease
   2. 고정값 변수 정리: 더 이상 사용되지 않는 MinNormalSpawnTime 변수를 삭제했어
   3. 결과: 이제 몬스터 난이도는 20초마다 스탯과 동시 스폰 개수가 1씩 증가하는 단일 규칙을 갖게 되었어

- 변수명 규칙 확인: [SerializeField] private 변수는 인스펙터에 보여지니까 Pascal Case를 사용하는 것이 유니티 환경에서 가독성을 높이는 실용적인 방법인걸 재확인하고 적용했어.
   

- EnemyDifficulty 스크립트에서 스폰 주기를 업데이트할 필요가 없어짐에 따라 불필요해진 변수와 함수를 EnemySpawn 스크립트에서도 제거하여 깔끔하게 정리했어.
   1. 함수 제거: 스폰 주기가 더 이상 변하지 않으므로, 외부에서 스폰 주기를 설정할 때 사용하던 함수 SetNormalSpawnTime(float newTime)을 삭제했어
   2. 주석 수정: private float normalSpawnTime 변수의 주석을 '계속 변할 거야'에서 '고정값' 으로 수정하여 현재 로직을 정확하게 반영했어
   3. 결과: EnemySpawn 스크립트는 오직 스폰 주기(고정)와 스폰 개수(20초마다 증가)라는 두 가지 정보를 받아 스폰 로직을 실행하는 역할만 맡게 되었지



11/15
플레이어가 나무 오브젝트 뒤에 있을 때 검은색 실루엣으로 보이도록 복사본 기반의 동기화 시스템을 구현하고 안정화
실루엣 오브젝트에 Sil_Player 스크립트 추가

- 실루엣 전용 오브젝트 생성: 원본 Player 오브젝트를 복사하여 Sill_Player 오브젝트를 생성
- 컴포넌트 제거 (핵심 안정화): Sill_Player에서 Rigidbody 2D, 콜라이더, 모든 로직 스크립트(Player Health, Attack Controller 등), Audio Source를 제거하여 복사본이 '시각적 역할'만 하도록 분리했어
- Sil_Player의 Sprite Renderer에 머트리얼을 생성 후 추가, 색상을 검은색으로 변경하고, Layer를 실루엣 전용 레이어로 지정.
- [핵심 렌더링 설정] 실루엣 효과가 활성화될 영역을 지정하기 위해 나무 오브젝트에 Sprite Mask 컴포넌트를 추가하여 실루엣 효과가 나타나도록 설정.
- Player의 Sprit Renderer의 Mask Interaction을 'Visible Outside Mask', Sil_Player는 반대로 'Visible Inside Mask' 로 지정.
- Sil_Player 스크립트 생성: 
   1. 변수: 원본의 Transform, Animator, SpriteRenderer를 받아올 변수 선언
   2. 동기화: LateUpdate() 함수 내에서 원본의 위치, Animator의 Move 상태, SpriteRenderer의 flipX(방향)를 복사하는 로직 구현
- Update()나 FixedUpdate() 대신 **LateUpdate()**를 사용하여 원본 플레이어의 움직임과 상태가 완전히 확정된 후에 복사하도록 했어 (실루엣의 위치와 상태가 떨림(Jittering) 없이 부드럽게 원본과 일치되도록 보장)


- 디버깅 및 안정성 확보 과정 (시간 소요의 주된 원인): 
- 문제 1: 몬스터 추적 오류 해결.
   1. 현상: 몬스터가 원본 대신 실루엣 복사본을 쫓아와 추적 위치가 어긋남
   2. 해결: 실루엣 오브젝트의 Tag를 Player_Sil로 변경하여 몬스터의 인식 대상에서 제외

- 문제 2: 사망 애니메이션 미작동 버그 해결:
   1. 현상: 모든 로직이 완벽해도 실루엣 기능이 작동하지 않았어
      1-1. LateUpdate 함수 동기화 로직과 Die 트리거의 충돌을 방지하기 위해 isSilDead 플래그 및 Animator.Play() 강제 실행 로직을 도입하고, Has Exit Time 등 애니메이터 설정을 모두 수정하여 기술적인 충돌 가능성을 완전히 제거**
      1-2. 이런 과정에도 실루엣은 Idle, Move 에니메이터는 작동 되지만 Die 만큼은 작동이 되지 않았어
   2. !발견!: Player 스크립트의 silplayer 변수(외부 오브젝트)가 인스펙터에 연결되지 않아 SilPlayerDie() 함수가 호출되지 않았음을 확인 (단순한 드래그 연결 미확인의 문제)
   3. 안정성 강화: Player 스크립트의 Start() 함수에 해당 변수가 Null일 경우 경고 메시지를 출력하는 디버그 로직을 추가하여 향후 연결 누락 오류를 방지
      3-1. Start 함수에 Null일때의 경고 메세지 추가. if (silplayer == null) Debug.LogWarning("Player: [실루엣 연결 누락!] sillplayer 변수가 인스펙터에 연결되지 않았어! 실루엣 기능이 작동하지 않을 거야!");

**이로써 11/2일에 적었던 문제를 드디어 해결했어. 단순히 Sorting Layer가 아닌 Sprite Mask와 나무에 가려질때 플레이어의 모습을 위한 추가 오브젝트가 필요했던거였어! 정말 수고 했어!!**
**내일은 공격 모션도 추가하자. 지금은 Idle, Move, Die만 실루엣에 적용된 상태니까, 물론 모션만 따라하는거지 실제로 공격이 나가서는 안되고!**
**내일은 다시 빌드해서 git에 추가하자**



11/16
플레이어 공격 모션 동기화 및 실루엣 시스템 완전 구현 (활, 검 공격 모션 추가)

- 어제 구현한 기본 모션(Idle, Move, Die)에 이어, 나머지 공격 모션(활, 검)까지 실루엣 오브젝트에 동기화하여 플레이어의 모든 애니메이션 상태를 나무 뒤에서도 완벽하게 보여주는 시스템을 완성했어!

- Sil_Player 스크립트에 활 (SilPlayerBowAttack),검 (SilPlayerSwordAttack) 공격 명령을 받을 일회성  함수 추가.
- Player 스크립트에 AttackController와 Sil_Player 스크립트를 연결하는 중개 함수 SilhouetteBowAttack(), SilhouetteSwordAttack() 추가.
- AttackController 스크립트에 공격 입력 시 Player 스크립트의 중개 함수를 호출하도록 로직을 수정하여, 원본 공격과 실루엣 동기화를 동시에 실행.


- 실루엣 공격 동기화 명령 흐름도
- AttackController(명령 시작)역할: 플레이어의 의도를 감지하고 공격 실행을 결정하는 주체.
   1. Update() 함수에서 Input.GetKeyDown(KeyCode.Space) (활 공격) 또는 KeyCode.Q (검 공격) 감지
   2. 쿨타임 체크 통과 (bowWeapon.CanAttack())
   3. weaponAnimator(WeaponType) 함수 호출
   4. 원본 애니메이션 실행: Animator.SetTrigger("Attack(Bow)");
   5. 실루엣 명령 전달: 자신이 알고 있는 Player 스크립트에게 "실루엣 공격 동기화 해달라구!"라고 명령을 위임
**if (Player != null) Player.SilhouetteBowAttack();**

- Player (명령의 중개자/총괄 관리)역할: AttackController와 Sil_Player 사이를 연결해 주는 통로 (낮은 결합도 유지) 역할. 실루엣 관련 모든 책임을 총괄
   1. AttackController로부터 SilhouetteBowAttack() 함수를 호출받아
   2. 자신이 이미 참조하고 있는 Sil_Player 객체가 존재하는지 안전하게 확인해
   3. 확인 후, Sil_Player에게 최종 명령을 전달
**if (silplayer != null) silplayer.SilPlayerBowAttack();//최종 명령을 Sil_Player에게 전달**

- Sil_Player (명령의 최종 실행)역할: 명령을 받아 실루엣의 애니메이션을 실제로 실행.
   1. Player 스크립트로부터 SilPlayerBowAttack() 함수를 호출받아
   2. LateUpdate()의 위치/이동 상태 동기화와 별개로, 일회성 애니메이션 트리거를 실행(LateUpdate는 위치,방향,이동만 담당)
**silplayerAnimator.SetTrigger("Attack(Bow)");// 실루엣 애니메이션 재생**


- 설계 원칙?
   1. 구조 분리 (LateUpdate vs. Public 함수): 지속적인 상태(Move, 위치)는 **LateUpdate()**로 동기화하고, 일회성 명령(공격, 사망)은 Public 함수로 명확하게 분리하여 애니메이션 충돌이나 버벅거림(Jittering) 없이 부드러운 작동을 보장
   2. 낮은 결합도 유지: AttackController 스크립트가 Sil_Player 스크립트를 직접 참조하지 않고 Player 스크립트를 **중개자**로 사용함으로써, 
   스크립트 간의 의존성을 낮추고(낮은 결합도) 향후 실루엣 시스템 변경 시에도 다른 스크립트의 수정 없이 안정적으로 유지보수할 수 있는 구조를 확립했어

**내일은 이 나무 뒤에 가려질때의 실루엣 구현에 대해 포트폴리오에 적어보자! (나무오브젝트의 Sprite Mask, 플레이어 복사본 오브젝트의 실루엣 역할로 분리 등등)**



11/17
노션 포트폴리오에 플레이어가 나무 뒤에 가져질떄 보일 실루엣 기능 설명

- 메인 주제 확정: 완성된 기능인 '장애물 투과 실루엣 시스템 구현 (Sprite Mask)' 을 포트폴리오의 '핵심 로직 구현 & 기술적 문제 해결' 섹션에 추가했어.
- 세부 파트 분리: 이 구현 과정을 시각적 문제 해결 (렌더링)과 로직 문제 해결 (동기화 및 설계)과 낮은 결합도 구조 설계: 기술적 세분화 (기술적 성과) 이라는 세 개의 큰 기술 파트로 명확하게 분류.

- 기술적 설계 원칙 상세 증명 (핵심 성과)
   1. 낮은 결합도 증명: 로직 해결 파트에서 '낮은 결합도(Loose Coupling) 구조 설계' 를 가장 큰 기술적 성과로 내세우고, 그 근거를 강화했어
   2. 코드 로직 배치: 이 낮은 결합도 구조가 어떻게 구현되었는지 보여주기 위해, 세 가지 핵심 스크립트(AttackController, Player, Sil_Player)의 위임 로직 코드 스니펫을 포트폴리오에 포함할 위치와 내용을 확정했어
   3. 책임 분리: LateUpdate()와 Public 함수를 분리한 이유(지속 상태 vs. 일회성 명령)를 다시 한번 정리하며, Sil_Player 스크립트 분리가 책임 분리 원칙(SRP)의 시작점임을 강조했지



11/18
if문 구문 간결화를 통한 가독성 개선 및 코드 정리

- 스크립트 전반의 포맷을 통일하고 가독성을 개선하기 위한 코드 클린업(Code Cleanup) 작업을 수행했습니다.
- 개선 목표: 단일 명령을 포함하는 if문 사용 시 불필요한 중괄호 {}를 제거하여 코드의 수직 밀도(Vertical Density) 를 높이고, 한눈에 파악 가능한 로직을 만듦.
- 적용 원칙:
   1. if (조건식) 명령문; 형태로 단일 명령은 한 줄로 간결하게 표현
   2. Debug.Log 호출, 여러 줄로 나뉘는 복합 명령, else 구문 등 가독성 저하 위험이 있는 경우에는 중괄호 {}를 그대로 유지하여 안전성을 확보

if (...) .....;
Debug.Log("...");                      X
else
Debug.Log("...");

if (...) ... Debug.Log("...); 
else ... Debug.Log("...);              O



11/19
플레이어에 대한 모든 스크립트의 로직의 코드를 품질 검사 및 유지보수
그 외의 스크립트의 주석 재확인 및 수정

- 새로운 기능을 추가하는 대신, 프로젝트의 장기적인 안정성을 확보하기 위해 핵심 로직(플레이어 관련 스크립트)에 대한 코드 품질 감사(Code Quality Auditing) 및 유지보수 작업을 집중적으로 수행
- 코드 클린업 (Clean Code): 단일 명령의 if 구문을 if (조건) 명령; 형태로 한 줄로 간결화하여 코드의 가독성 및 밀도를 향상 (11/18에 시작된 작업을 마무리 한거야)

- Arrow 스크립트의 OnTriggerEnter2D 함수 재확인.
   1. else if (other.CompareTag("Ground")) Destroy(gameObject); 화살이 타일맵 경계선에 닿으면 사라지는거야
   2. 플레이어랑 발사된 화살은 타일맵(Tag:Ground) 안에 있어. Enter 함수는 콜라이더에 닿아야 발동이 되는거잖아?
   3. 즉, 타일맵(Tag:Ground) 안에 있어서 OnTriggerEnter2D의 상황이 아냐! 타일맵의 콜라이더 밖 (경계선)에 화살이 닿으면 그때 함수가 작동해 화살이 파괴돼, 화살의 안에서 밖으로 콜라이더 경계선에 닿는 차이일뿐이야

- PlayerStatsEffects 스크립트의 각종 컴포넌트를 void Awake()로 이동.
   1. 같은 오브젝트의 내부 컴포넌트 참조는 Awake() 함수에서, 다른 외부 스크립트(플레이어 관련 스크립트가 아닌 외부의 스크립트)의 Start()가 실행되기 전에 참조가 완료되어 안정성이 보장
   2. 그래서 Player, PlayerHealth, BowWeapon등 플레이어 오브젝트의 컴포넌트 스크립트들을 Awake() 함수에 추가했어
   3. Start함수에는 아이템을 획득하면 Lv. UI를 업데이트 하는 UpdateWeaponLevelUI 함수만 남았지(게임이 가장 처음 시작하는 시점에 최소한의 기본값을 화면에 표시하기 위한 필수적인 초기 설정)
      3-1. UpdateWeaponLevelUI 함수에선 currentArrowLevel, currentSwordLevel, currentMoveSpeedLevel 값의 0인 상태로 시작해야 하니까
      3-2. Update() 함수에 두면 매 프레임(초당 수십 번) 불필요하게 UI를 갱신해서 성능 낭비야, 
      Start()는 게임 오브젝트가 활성화된 후 첫 번째 프레임 직전, 단 한 번만 실행되는 유니티의 생명 주기 함수이기 때문에, **게임 시작 시 한 번의 초기 설정**을 위한 가장 적절하고 안전한 위치가 돼
      3-3.그래서 UpdateWeaponLevelUI 함수를 바꾸는게 아닌 그 안의 레벨 숫자를 바꾸는 current...Level 변수가 증가면 그 값만 변하게 하면 돼

- 삼항 연산자(?:) 사용의 당위성 확인
- 삼항 연산자는 필수는 아냐! 단순한 값 결정 로직에는 간결하여 효율적이지만, if문과 섞어 쓰면 이해하기 어렵고 복잡하거나 다른 동작(사운드 등)을 추가할 때는 일반 if-else가 더 유연하고 가독성이 높여.
- "이런 방식으로도 가능하다" 라는걸 보여주기 위해 추가한것일 뿐이야.

- 포트폴리오의 '몬스터 추적 오차 해결 과정' 문제 해결 토글의 문제 해결 전 gif 이미지 밑에 문제 해결의 요약을 적어, 토글 최상단에 요약 문장 배치를 결정하고, 
  내용에 FixedUpdate, Collider.bounds.center, OffsetY, 방어적 코딩 등 기술 키워드를 강조하여 프로그래머 역량을 어필.
- "몬스터 추적 로직의 문제 해결: 시각적 디자인 스케일링에 따른 추적 오차를 Collider.bounds.center와 OffsetY 조합으로 보정하고, FixedUpdate 및 방어적 코딩으로 물리 연산의 안정성과 성능 최적화를 달성"



11/20
몬스터와 아이템 관련 스크립트 전반의 코드 스타일을 통일하고 가독성 및 안정성을 극대화 (중괄호 생략, 단일 표현식 최적화)

- Enemy, Difficulty = 짧은 if문은 그 if문의  한 줄에 위치시켜 가독성, 및 간결화 (디버그 등 긴 코드면 그대로 한칸 밑에 둬서 유지)
- EnemySpawn = GetValidSpawnPosition() 내 if 문을 한 줄로 정리.
- EnemyHealth = TakeDamage() 내 널 체크 및 단일 if 문 간결화.

- ItemChest = OpenChest() 함수 내 콜라이더 비활성화 및 스프라이트 교체 로직 간결화.
- ItemChestSpawner = Start() 및 SpawnItemChest() 내 모든 단일 if 문을 한 줄로 정리.
- ItemSpawner = 한 줄만 있는 함수인 ItemDestroyed() 함수를 표현식 본문 (=>)으로 최종 간결화.

**스크립트 전반의 코드 통일성이 확보, 단일 명령문에 대한 불필요한 중괄호를 제거하여 코드 라인 수가 줄고 가독성이 높아졌어, 몬스터 및 아이템 스폰/획득 로직의 안정성 유지 확인**



11/21
게임 내 핵심 매니저 스크립트(6종)의 코드 스타일을 간결화하고, 널 체크 및 상태 제어 로직의 안정성을 강화했어. (코드 가독성 향상)

- 코드 스타일 간결화.
   1. 대부분의 함수에서 단일 명령문 if 블록의 중괄호({})를 제거하고 한 줄로 정리
   2. GameTimerUI 스크립트의 StopTimer() / ResumeTimer() 함수를 람다식 (=>) 으로 변환하여 코드를 최소화했어

- 흐름 제어 및 안정성 강화.
   1. OptionsManager 스크립트의 ToggleOptionsPanel() 함수에 가드 절(Guard Clause) 패턴을 적용하여, 카운트다운 중 옵션 활성화를 return;으로 깔끔하게 차단
   2. GameOverManager 스크립트의 DisplayFinalScore()의 널 체크 로직 if, else if를 중괄호 제거해 코드량을 줄였어
   3. OptionsManager 스크립트의 시간 재개 로직을 else if (CountdownManager.isCountdownFinished)로 정리하여, 카운트다운 완료 후에만 게임 시간이 재개되도록 안정 장치를 확보

- 싱글톤 로직 확인
   1. ObstacleDifficultyManager 스크립트의 Awake() 함수 내 싱글톤 및 DontDestroyOnLoad 로직의 필요성을 재확인하여 씬 전환 시 안정성을 보장함, 만약에 있을 중복된 스크립트 오브젝트로 인한 오류를 방지하기 위해



11/22
Sil_Player 실루엣 오브젝트의 애니메이션 동기화 로직 최적화 (LateUpdate로 상태 복사)

- 동기화 구조 변경: 실루엣(Sil_Player)의 애니메이션 동기화 방식을 기존의 '개별 함수 명령(SetTrigger)' 방식에서 'LateUpdate 함수를 사용한 원본 상태 통째로 복사' 방식으로 전면 변경.
- 불필요한 로직 제거: AttackController 및 Player 스크립트에서 실루엣(Sil_Player)으로 공격 및 사망 명령을 보내는 모든 불필요한 함수 호출 로직(Player.SilhouetteBowAttack() 등)을 모두 제거해서 코드 복잡성과 의존성을 해소했어.
- 최적화된 구현: Sil_Player 스크립트의 LateUpdate()에 Animator.Play(hash, layer, normalizedTime) 로직을 추가해, **모든 애니메이션 상태(이동, 공격, 사망 등)**를 시간 오차 없이 완벽하게 동기화하도록 구현.
**나중에 새로운 무기나 동작을 추가할 때 원본 로직과 관련 스크립트, 실루엣의 스크립트와 에니메이터를 수정할 필요가 없어졌으며, 가장 효율적이고 안정적인 실루엣 복제 시스템을 구축한거야. (기존의 'OnAttackEnd' 같은 애니메이션 이벤트 수신기 오류는 무시)**



11/23
Sil_Player 스크립트의 LateUpdate()에서 사용한 Animator 시스템 API 정리
메인메뉴 씬 관련 스크립트 정리

```for (int i = 0; i < playerAnimator.layerCount; i++)
    //1. 원본 플레이어가 '현재' 어떤 상태인지 파악(걷는 중인지, 공격 중인지, 사망했는지)
    AnimatorStateInfo stateInfo = playerAnimator.GetCurrentAnimatorStateInfo(i);
    //[실루엣에게 복사 명령] 실루엣 애니메이터에게 원본과 똑같이 따라 하라고 명령해.
    silplayerAnimator.Play(stateInfo.fullPathHash,//(무엇을?)원본이 지금 하는 그 애니메이션의 이름표(ID)
                                                i,//(어디서?)원본과 같은 레이어(층)에서
        stateInfo.normalizedTime);//(어디부터?)원본이 진행된 그 지점(%)부터 이어서 재생해서 끊김 없는 부드러운 동기화```

- stateInfo(지역변수) = stateInfo는 원본 플레이어가 현재 걷고 있는지, 공격 중인지, 아니면 사망 모션의 몇 % 지점에 있는지에 대한 모든 데이터를 담고 있어.
- AnimatorStateInfo = 현재 재생 중인 애니메이션 상태에 대한 모든 정보(애니메이션 이름 ID, 진행 시간, 속도 등)를 담고 있는 유니티의 공식 자료형.
   1. 코드 역할: 이 객체를 통해 원본 플레이어의 **fullPathHash**와 normalizedTime 정보를 추출할 수 있어
- GetCurrentAnimatorStateInfo(i) = Animator 컴포넌트에 "지금 i 번째 레이어에서 재생하고 있는 애니메이션의 **AnimatorStateInfo**를 줘!" 라고 명령하여 현재 상태 정보를 가져오는 함수.
   1. 코드 역할: 실루엣이 따라 하기 위해, 원본 플레이어의 현 상태 정보를 추출하는 시작점
- Animator.Play(hash, layer, time) = Animator에게 지정한 애니메이션 상태를 즉시 시작하라고 강제 명령하는 함수. SetTrigger()처럼 연결선(Transition)의 조건이나 대기 시간을 무시하고 바로 재생시켜.
   1. 코드 역할: 실루엣이 원본의 상태를 매 프레임 지연 없이 덮어써서 재생하게 만드는 핵심 명령
- fullPathHash = 애니메이션 상태의 **이름(문자열)**을 숫자로 바꾼 고유 ID (해시값). 유니티는 문자열 이름 대신 이 ID를 사용하면 매우 빠르게 애니메이션을 찾고 재생할 수 있어 성능 최적화에 필수적이야.
   1. 코드 역할: Play() 함수의 첫 번째 인자로 사용되어 무엇을 재생할지 정확하게 지정
- normalizedTime = AnimatorStateInfo 안에 들어있는 정보 중 하나로, 현재 애니메이션이 0.0(시작)에서 1.0(끝) 중 어느 지점까지 진행되었는지 알려주는 비율 값.
   1. 코드 역할: Play() 함수의 세 번째 인자로 사용되어, 실루엣이 원본 애니메이션을 **정확히 같은 지점(시간)**부터 이어서 재생하게 만들어 부드러운 동기화를 보장

- for (int i = 0; i < playerAnimator.layerCount; i++) = 원본 플레이어의 Animator에 설정된 **Layer 0 (기본 동작)** 부터 마지막 Layer까지, 모든 애니메이션 층을 빠짐없이 검사하기 위해 반복문을 시작해.
- AnimatorStateInfo stateInfo = playerAnimator.GetCurrentAnimatorStateInfo(i); = 현재 순회 중인 i 번째 Layer에서, 원본 플레이어에게,
  "지금 이 순간 재생 중인 애니메이션의 모든 정보(ID, 진행 시간 등)" 를 담은 꾸러미(AnimatorStateInfo)를 달라고 요청하고 받아와.
- stateInfo.fullPathHash = 받아온 꾸러미에서 "재생할 애니메이션의 이름표(고유 ID)"를 추출해. 이 ID가 실루엣이 따라 할 '무엇을' 나타내.
- stateInfo.normalizedTime = 꾸러미에서 "애니메이션이 현재 몇 % 지점까지 진행되었는지"를 나타내는 비율 값(0.0 ~ 1.0)을 추출해. 이 지점이 실루엣이 재생을 시작할 '어디부터'를 나타내.
- silplayerAnimator.Play(hash, i, normalizedTime); = 실루엣의 Animator에게 추출한 정보를 이용해 즉시 재생을 강제 명령해.
   1. 무엇을: fullPathHash가 가리키는 애니메이션을
   2. 어디서: 원본과 같은 i 번째 Layer에서
   3. 어디부터: 원본이 진행된 normalizedTime 지점부터 이어서 재생해서, 끊김 없이 부드럽게 동기화돼


- 메인메뉴 씬 관련한 모든 스크립트를 정리했어.
- BackgroundScroller 스크립트의 RawImage rawImage = GetComponent<RawImage>(); 코드를 Start() 대신 Awake() 함수로 옮겨서, 스크립트가 로드되자마자 내부 컴포넌트를 가장 빠르고 안전하게 참조하도록 수정.
- MainMenuEnemy, MainMenuPlayer 스크립트는 수정 불필요.
- MainMenuCutsceneManager 스크립트는 플레이어, 몬스터의 각 case당 속도 변수(예: Cutscene1pSpeed)와 SetState() 함수 내의 `반복적인 코드가` 확장성과 유지보수성을 저해,
  그래서 내일 이 스크립트를 정리할거야.



11/24
MainMenuCutsceneManager 스크립트에 4번째 씬(case)를 추가. (플레이어가 활 공격을 하며 몬스터를 쫒아가)
활 공격을 위해 화살 프리팹, 화살 스크립트 추가

- CutsceneSpeedData 구조체 활용: 
   1. struct를 정의하여 플레이어, 몬스터1, 몬스터2, 엘리트 몬스터의 속도를 하나의 묶음으로 관리하도록 했어,
   이로써 인스펙터에서 각 컷신(Cutscene1~Cutscene4) 별 속도 데이터를 한눈에 확인하고 수정할 수 있게 되어 관리 효율이 높아졌지.

- 4번째 상태에선 모든 몬스터가 오른쪽에서 왼쪽으로 도망가. 플레이어도 그 뒤를 쫒아가지만 이곳엔 검공격 대신 활 공격을 하게 했어.
   1. ChasePlayer2 상태(3번째 case) 가 ChangeStateX끝에 도달하면(ChangeStateX값) → PlayerAttack2로 전환되도록 연결
   2. PlayerAttack2 상태가 ReturnPositionX에 도달하면 → 다시 첫 상태인 ChasePlayer로 돌아가도록 연결하여 무한 루프 구조를 완벽하게 구현

- 활 공격 연출 기능 (화살 발사) 추가.
- 게임씬의 화살 프리팹과 화살 스크립트를 복사해서 가져왔어. 하지만 메인화면씬에선 다르게 써야 하니 화살 스크립트의 내부 로직을 크게 수정했어.
   1. 게임 로직(데미지, EnemyHealth 참조)을 모두 제거하고, 컷신에 필요한 단순 이동 및 수명 관리, Enemy 태그 오브젝트에 닿으면 사라지는 기능만 남겨 컷신 전용 스크립트로 최적화했어
   2. 화살의 이동 방식(transform.Translate)에서 발생하는 방향 오류를 해결하기 위해, 월드 좌표 기반 이동 transform.position += new Vector3(moveAmount, 0, 0); 방식으로 수정하여,
   direction 변수값(화살 방향 (1: 오른쪽, -1: 왼쪽))에 따라 정확히 좌우로 날아가도록 수정
**float moveAmount = direction * MoveSpeed * Time.deltaTime; 은 direction방향변수 (1, -1), MoveSpeed화살의 속도 변수, Time.deltaTime은 이전 프레임에서 현재 프레임까지 걸린 시간을 합친 변수야**
**transform.position += new Vector3(moveAmount, 0, 0); 는 위치 업데이트. `화살의 새로운 위치는 = 현재 위치 + X축 방향의 이동 거리` (X축으로만 moveAmount만큼 움직이겠다는 벡터 값이야, Y와 Z는 0이므로 이동 없음)** 

- 메인메뉴 플레이어 스크립트 (MainMenuPlayer.cs) 기능 추가.
   1. MainMenuPlayer 스크립트에 ShootArrow() 함수를 추가하여 화살 프리팹을 동적 생성 (Instantiate)하도록 구현
   2. 화살 생성 직후, 플레이어의 현재 방향 (this.direction)에 맞춰 화살의 이동 방향과 시각적 회전 (Y축 180도)을 동시에 설정하여 연출의 완성도를 높임
   3. 플레이어의 활 공격 애니메이션 클립 중 시위를 놓는 프레임에 ShootArrow() 함수를 연결해서 모션과 발사 타이밍을 정확하게 일치시켰어



11/25
본가에 다녀왔어



11/26
프로젝트 에셋 구조를 씬별로 분리하여 가독성 및 관리 효율 극대화
몬스터 난이도 상승 밸런스를 조정하고 빌드 환경 오류를 해결하여 안정적인 빌드 파이프라인을 확보.

- Assets/Cha 폴더 내의 모든 리소스(프리팹, 사운드, 이미지 등)를 `GameScene`과 `MainScene` 폴더 안으로 각각 이동시켜 구조를 명료화했어.
   1. 프로젝트 탐색 시 핵심 씬 폴더만 노출되도록 하여 파일 관리가 훨씬 수월해졌지
   2. 기존에는 Cha 폴더 안에 Scenes 폴더 안에 메인화면 씬, Cha폴더에 게임씬이 흩어져 있어서 찾는게 번거로웠어


- EnemyDifficulty 스크립트에서 SpeedIncreaseRatio 변수 값을 0.5f로 상향 조정하여 20초마다 몬스터의 이동 속도가 확실히 빨라지도록 변경.
   1. 하지만 속도 증가로 인해 발생한 몬스터 이미지의 떨림 현상을 확인했어
   2. 임시 해결책으로 SpeedIncreaseRatio 값을 **0.15f**로 낮춰 떨림 현상 없이 부드럽게 움직이도록 밸런스를 일시적으로 조정 (스크립트와 인스펙터에 값을 똑같이 수정, 인스펙터의 값과 스크립트의 값이 다르면 혼란하니)


- 빌드 오류 해결: Windows 빌드 시 Unity.Android.Gradle 네임스페이스가 참조 오류를 일으키는 현상을 파악.
   1. MainMenuCutsceneManager 스크립트에서 **using Unity.Android.Gradle;** 네임스페이스 줄을 삭제하여 불필요한 플랫폼 종속성을 제거하고 빌드 오류를 해결



11/27
몬스터 이동 시 떨림(Jittering) 현상의 원인을 파악하고 해결
몬스터 난이도 (이동속도)밸런스 확정 및 계산 방식 명료화

- 몬스터의 이동속도가 증가할때 발생하던 떨림 현상의 원인은 코드 문제가 아닌, 유니티의 물리 업데이트(FixedUpdate) 주기와 화면 렌더링(Update) 주기의 불일치 때문이었어.
- 해답은 몬스터 프리팹의 Rigidbody2D 컴포넌트에서 Interpolate (보간) 설정을 None에서 **Interpolate**로 변경이었어.
**원리 이해: 유니티가 이전 물리 위치와 현재 물리 위치 사이를 예측하여 화면에 부드럽게 그려주도록 설정하여 떨림 현상을 코드 수정 없이 해결**


- 20초마다 몬스터의 강화 시스템 중 이동속도의 증가값이 "현재 속도의 0.2f(20%)일까? 아니면 몬스터의 기본 속도의 20%만일까?" 중 어떤것인지 햇갈렸어.(EnemyDifficulty 스크립트에서 인스펙터의 SpeedIncreaseRatio 값을 0.2f로 수정)
- 몬스터 이동 속도 증가 방식이 복리(현재 속도의 20% 증가)가 아닌, 기본 이동 속도의 20%씩 선형적(Linear) 으로 증가하는 방식임을 재확인했어.
**증가 비율(0.2f)이 곱해지는 대상이 `몬스터의 기본 속도` 라는 거야. 매번 기본 속도에 20%씩 더해주는 선형적인 증가 방식이야**

- 내 스크립트의 계산 공식은 `조정된 속도 = 기본 속도 X (1+증가 비율X현재 레벨)`
   1. 조정된 속도(adjustedStat) = Enemy 스크립트의 currentMoveSpeed 변수에 최종적으로 저장되어 몬스터의 이동에 사용되는 값
   2. 기본 속도(baseStat 함수 매개변수) = Enemy 스크립트의 EnemyStats 클래스 MoveSpeed에 처음 설정된 몬스터의 초기 이동 속도
   3. 1f + = 기본 스탯 $100%를 의미. 이 수식에서 1f가 없다면, 기본 속도는 증가된 부분만 곱해져서 원래 스탯이 사라져 버려. 즉, 1f는 "원래 스탯을 기준으로" 증가분을 더하겠다는 뜻이야, 
   뒤의 **increaseRatio * currentDifficultyLevel**은 누적된 증가율(0.2 XLevel)을 의미해
   4. 증가 비율(SpeedIncreaseRatio) = [SerializeField] private float SpeedIncreaseRatio = 0.2f; 난이도 레벨당 기본 속도에서 몇 퍼센트(0.2f = 20%)를 증가시킬지 설정하는 값
   5. 현재 레벨(currentDifficultyLevel) = private int currentDifficultyLevel = 0; 20초마다 1씩 증가하며, 난이도 상승 횟수를 나타내는 변수


**내일은 몬스터가 플레이어의 범위(콜라이더)에 도달하면 멈추고 공격 모션을 하는게 아닌 범위에 닿으면 이동을 멈추지 않고 공격. 즉, 중간에 멈추지 않고 움직이면서도 공격 모션을 하게 해보자**
**그리고 오늘 했던 몬스터 떨림 현상을 수정했으니 내일은 다시 빌드해서 git과 포트폴리오에 올리자**